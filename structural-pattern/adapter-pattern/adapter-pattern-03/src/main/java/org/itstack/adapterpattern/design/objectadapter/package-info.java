/**
 * 【对象适配器优点】
 * <ol>
 *     <li>对象适配器和类适配器其实算是同一种思想，只不过实现方式不同。根据合成复用原则，使用组合替代继承，所以它解决了类适配器必须继承src的局限性问题，也不再要求dst必须是接口</li>
 *     <li>使用成本更低，更灵活</li>
 * </ol>
 * 
 */
package org.itstack.adapterpattern.design.objectadapter;