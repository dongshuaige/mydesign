/**
 * 总结<br>
 * 类适配器：以类给到，在Adapter里，就是将src当做类，继承<br>
 * 对象适配器: 以对象给到，在Adapter里，将src作为一个对象，持有（聚合）<br>
 * 接口适配器: 以接口给到，在Adapter里，将src作为一个接口，实现Adapter模式最大的作用是将原本不兼容的接口融合在一起工作<br>
 * 实际开发中，实现起来不拘泥于上述三种经典形式
 */
package org.itstack.adapterpattern.design;